import admin from './admin'
import example from './example'
import manager from './manager'
import user from './user'

const menu = {
  "ADMIN": admin,
  "USER": user,
  "MANAGER": manager,
  "EXAMPLE": example
}

export default menu
