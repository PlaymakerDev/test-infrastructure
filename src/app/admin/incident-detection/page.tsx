import React from 'react'
import IncidentDetectionScreen from '@/features/admin/incident-detection/overall/screen'

const IncidentDetectionPage = () => {
  return <IncidentDetectionScreen />
}

export default React.memo(IncidentDetectionPage)
