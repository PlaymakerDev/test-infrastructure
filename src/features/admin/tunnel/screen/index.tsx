"use client"
import React from 'react'
import ComingSoon from '@/components/common/ComingSoon'

const TunnelScreen: React.FC = () => {
  return <ComingSoon title="Tunnel" />
}

export default React.memo(TunnelScreen)
