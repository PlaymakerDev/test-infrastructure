/**
 * Shared pagination configuration for every Settings table.
 * Kept in one place so page-size dropdowns and default limits stay
 * in lockstep across Project / Route / Contact / User sections.
 */
export const PAGE_SIZE_OPTIONS = ['20', '50', '100', '200']
export const DEFAULT_PAGE_SIZE = 100
